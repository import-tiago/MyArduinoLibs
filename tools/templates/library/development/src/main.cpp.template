#include <Arduino.h>
#include <__LIBRARY_NAME__.h>

__LIBRARY_NAME__ library;

void setup() {
    Serial.begin(115200);

    if (!library.begin()) {
        Serial.println("Unable to initialize __LIBRARY_NAME__.");
        while (true) {
            delay(1000);
        }
    }
}

void loop() {
    delay(1000);
}
